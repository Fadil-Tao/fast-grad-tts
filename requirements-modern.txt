# Modern deps (originals in requirements.txt target py3.6 / old pins).
# torch + torchaudio come preinstalled on Colab; on a bare machine install a
# CUDA torch first, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# then: pip install -r requirements-modern.txt
#
# numpy is intentionally NOT pinned: the repo uses no removed numpy-1 APIs, and
# forcing numpy<2 conflicts with Colab's preinstalled numpy-2 stack (torch/numba).
Cython>=0.29
numpy
matplotlib>=3.5
einops>=0.6
inflect>=6.0
Unidecode>=1.3
librosa>=0.10
scipy>=1.10
tqdm
