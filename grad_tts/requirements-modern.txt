# Modern, py3.10/3.11-compatible deps (originals in requirements.txt target py3.6).
# Install torch SEPARATELY for your CUDA version first, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu121
# then: pip install -r requirements-modern.txt
Cython>=0.29
numpy<2.0          # numba/librosa + legacy np.float usage need numpy 1.x
matplotlib>=3.5
einops>=0.6
inflect>=6.0
Unidecode>=1.3
librosa>=0.10
scipy>=1.10
tqdm
